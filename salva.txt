// Pega a primeira planilha
    const ws = wb.Sheets[wb.SheetNames[0]];

    // Pega o número de linhas existentes na planilha
    const range = XLSX.utils.decode_range(ws['!ref']);
    const numLinhasExistentes = range.e.r + 1;

    // Adiciona os novos dados abaixo das linhas existentes
    valor.forEach((linha, i) => {
      linha.forEach((celula, j) => {
        ws[XLSX.utils.encode_cell({r: numLinhasExistentes + i, c: j})] = { v: celula };
      });
    });